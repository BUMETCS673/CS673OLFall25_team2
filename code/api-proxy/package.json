{
  "name": "careerforge-api-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Simple HTTPS proxy that forwards /api requests to the existing HTTP backend to avoid browser mixed content blocking.",
  "scripts": {
    "start": "node server.mjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "http-proxy-middleware": "^3.0.0",
    "morgan": "^1.10.0"
  }
}
